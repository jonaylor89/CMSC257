
IP = argv[1]
PORT = argv[2]
FILENAME = argv[3]

mySocket = socket(PF_INET, SOCK_STREAM)

connect(mySocket, IP, PORT)

write(mySocket, "GET " + FILENAME)

myFile = open(FILENAME)

myBuffer[50]

n = read(mySocket, myBuffer, 50)

while myBuffer is not "/cmsc257"
  append(myFile, myBuffer) 
  n = read(mySocket, myBuffer)
endwhile

close(myFile)

close(mySocket)
